const { generateQrisString } = require('../src/lib/doku.ts'); // Wait, src/lib/doku.ts is a TS file, we can't directly require it in Node unless we use tsx or ts-node.
// Let's copy the function body or run it using tsx!
